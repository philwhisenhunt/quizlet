<div id="<%= dom_id quiz %>" class="rounded-box">

  <tr>
    <strong><%= link_to quiz.name, quiz %></strong>
    <br>
    <hr>
   
    <td><%= link_to "Edit", edit_quiz_path(quiz) %></td>
    <td><%= link_to "Destroy", quiz, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    <br>
    <td><%= link_to "Take Quiz", session_maker_path(quiz) %></td>
  </tr>


</div>
